// Re-export from useVisitors for backwards compatibility
export {
  useVisitors as useTestdata,
  useVisitorById as useTestdataById,
  useAddVisitor as useAddTestdata,
  useUpdateVisitor as useUpdateTestdata,
  useDeleteVisitor as useDeleteTestdata,
} from './useVisitors';